docker run -d --rm --name nginx -p 80:80 nginx
docker run -d --rm --name httpd -p 3000:80 httpd
docker run -d --rm --name mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=wednesday  mysql
docker container inspect mysql

docker container run --rm -it --name nginx nginx bash

docker container run -it --name ubuntu ubuntu bash
docker container exec -it ubuntu bash
docker stop ubuntu
docker rm ubuntu

docker container run -d --name characters alpine:3.7.3 /bin/sh -c "while :; do wget -qO- https://example.com; printf '\n'; sleep 5s; done"

docker network create -d bridge round-robin

docker container run -d --net-alias round-robin elasticsearch:2